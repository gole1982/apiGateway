# apiGateway —— Docker 部署环境变量模板
# 用法：cp .env.example .env，然后填值。本文件可提交（不含任何真实密钥）。

# 主密钥（生产必须）：64 位 hex（32 字节）。容器重建不丢 key 全靠它 ——
# settings.sb_api_key 与平台 token 都是用它加密存库的。
# 生成：openssl rand -hex 32
# 注意：从 ~/.apiGateway.key 迁移过来时，把文件里的 hex 原样复制到这里，
# 否则历史密文解不开（见 internal/crypto/crypto.go）。
APIGATEWAY_KEY=
