.aider*
.gocache/
*.exe
*.db
*.db-shm
*.db-wal
logs/
.traerules

# tooling scratch / probe artifacts
.zcode*
*_probe_*.txt
*.zcodecliexec*
build_err.txt
glm_verify.txt

# DB backups
*.db.bak*
.backup/

# CI build output
bin/

# local tooling data/config (never commit)
.freebuff/
.workbuddy/
.workbuddy-ai/
*.exe~
opencode.json

# 本地数据备份（bundle 契约 v2 迁移前快照，勿入库）
backups/
