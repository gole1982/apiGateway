# API网关代理系统 - 需求文档

## 1. 项目目标

### 1.1 项目结束标志
- 实现多用户共享的API网关代理服务
- 支持多种API格式（OpenAI / Anthropic / Gemini）的客户端和上游
- 支持动态令牌刷新和故障转移
- 提供可视化管理界面
- 实现调用统计和监控

### 1.2 用户操作方式
- **管理员**：通过Web界面配置Platform和RAPI、创建LAPI、编排路由规则、探测API格式、查看统计
- **终端用户**：通过标准API接口调用，只需填写LAPI别名作为model字段，无需关心后端细节

### 1.3 项目成果（新增能力）
| 能力 | 描述 |
|------|------|
| 统一入口 | 用户只需填写LAPI别名，无需关心后端地址、模型名或协议差异 |
| 多协议兼容 | 同时接受OpenAI、Anthropic、Gemini格式的客户端请求，向任意格式的上游转发 |
| 故障转移 | 自动从主服务切换到备用服务，对客户端完全透明 |
| 动态令牌 | 自动刷新过期令牌，服务不中断 |
| 智能选路 | 按Cost排序、时段规则、用量阈值自动选择最优后端 |
| 统计分析 | 可视化展示API使用情况 |

---

## 2. 项目概述

### 2.1 背景（为何要做）
- 存在多个LLM服务提供商（如OpenAI、Anthropic、Google Gemini等）
- 不同提供商使用不同的API协议，客户端需要适配多种格式
- 缺少统一的入口和故障转移机制
- 令牌过期需要手动更新，影响业务连续性

### 2.2 现状（目前情况）
| 现状 | 描述 |
|------|------|
| 分散管理 | 各团队独立配置API，缺乏统一管理 |
| 单点故障 | 服务中断时需要手动切换 |
| 静态令牌 | 令牌过期需人工更新 |
| 协议不统一 | 不同提供商API格式各异，客户端适配成本高 |
| 缺乏监控 | 无法直观了解API使用情况 |

### 2.3 缺失能力
- 统一的API入口（屏蔽后端协议差异）
- 自动故障转移
- 动态令牌管理
- API格式自动探测与转换
- 可视化统计监控

---

## 3. 核心需求

### 3.1 Platform管理
| 需求 | 描述 |
|------|------|
| 配置服务商 | 支持配置上游服务商的基础URL |
| 多Key负载分担 | 同一平台可配置多个有序API Key；调用时按序选取第一个非冷却Key；某Key受限时仅对该Key冷却，同平台其他Key不受影响；全部Key受限才判定该平台不可用 |
| 支持动态令牌 | 支持通过外部命令获取和刷新令牌（通常配合单Key使用） |
| API格式探测 | 配置平台时，可主动探测该平台支持哪些API格式（OpenAI / Anthropic / Gemini） |
| 探测远程模型列表 | 向平台发起 GET /v1/models 请求，获取该平台支持的模型名称列表，供管理员选择后批量创建RAPI |
| 批量创建RAPI | 管理员从探测到的模型列表中勾选，一键批量创建RAPI（含平台级格式探测） |

### 3.2 RAPI管理
| 需求 | 描述 |
|------|------|
| 添加远程API端点 | 支持配置远程服务的模型名、所属平台、Cost参数 |
| 支持格式声明 | 每个RAPI记录该端点支持的API格式列表（JSON数组） |
| 格式自动探测 | 可对单个RAPI触发格式探测并自动保存结果 |
| Cost配置 | 支持配置BaseCost、HighCost、用量阈值（RPM/RPH/RPD/TPM/TPH/TPD）和时段规则 |

### 3.3 LAPI管理
| 需求 | 描述 |
|------|------|
| 创建虚拟API | 为用户提供统一的接口别名（alias强制小写存储） |
| 配置路由链 | 支持多个RAPI组成故障转移链，可配置优先级顺序 |
| 启用/禁用 | 支持管理员手动启用或禁用LAPI |

### 3.4 请求路由
| 需求 | 描述 |
|------|------|
| 查询可用模型 | 客户端可通过 GET /v1/models 获取当前网关所有已启用LAPI的列表，返回OpenAI兼容格式 |
| Model字段路由 | 根据请求中model字段（大小写不敏感）匹配LAPI别名 |
| 多协议接入 | 根据URL路径自动识别客户端使用的API格式 |
| 格式透明转换 | 选定RAPI后，若客户端格式与RAPI支持格式不一致，执行一步直接转换；一致则零转换透传 |
| 模型名替换 | 转发前将请求体中的LAPI别名替换为RAPI的真实模型名 |
| 请求体保留 | 客户端body在请求成功响应或所有RAPI判定不可用前始终保留在内存中，支持无损重试 |
| 自动故障转移 | 主服务失败时自动尝试备用服务，对客户端透明 |

### 3.5 令牌管理
| 需求 | 描述 |
|------|------|
| 双重缓存 | 内存+数据库缓存令牌，同Platform下所有RAPI共享 |
| 定时刷新 | 后台定时更新令牌（默认10分钟） |
| 被动刷新 | 收到401错误时立即刷新并重试同一RAPI一次，仍失败则切换下一个 |

### 3.6 统计监控
| 需求 | 描述 |
|------|------|
| 调用统计 | 记录调用次数、成功/失败数（分401/429/500/其他） |
| 性能统计 | 记录响应时间、Token使用量 |
| 按LAPI汇总 | 聚合展示LAPI下所有RAPI的统计 |
| 请求趋势 | 按分钟粒度记录请求量，支持最近N分钟趋势图 |

---

## 4. 用户案例

### 案例1：管理员配置远程服务
**目标**：添加Anthropic Claude服务到网关  
**步骤**：创建Platform → 探测支持格式 → 拉取模型列表 → 批量创建RAPI  
**成果**：RAPI配置完成，支持的API格式已自动记录，可被LAPI引用

### 案例2：管理员创建统一入口
**目标**：为开发团队创建统一接口"claude-sonnet"  
**成果**：LAPI创建完成，配置了OpenAI官方账号（主）+ Azure备用的故障转移链

### 案例3：终端用户调用（格式透明）
**目标**：Cursor（使用Anthropic SDK）通过网关调用实际部署在OpenAI格式上游的模型  
**成果**：网关自动将Anthropic格式请求转为OpenAI格式转发，将OpenAI格式响应转回Anthropic格式回复，用户感知不到协议差异

### 案例4：故障转移生效
**目标**：主服务超限（429）时自动切换  
**成果**：请求自动路由到备用服务，业务不中断，客户端收到正常响应

### 案例5：令牌自动刷新
**目标**：企业内网SSO令牌过期时自动更新  
**成果**：令牌刷新成功，服务不中断，客户端连接保持

---

## 5. 非功能需求

| 需求 | 描述 |
|------|------|
| 并发支持 | 支持100+并发请求 |
| 低延迟 | 网关自身处理延迟 < 500ms（不含上游响应时间） |
| 跨平台 | 支持Windows/Linux/macOS |
| 快速故障转移 | 切换时间 < 1秒（不含上游响应时间） |
| 单二进制部署 | 无外部依赖，单文件可运行 |
| 流式支持 | 支持SSE流式响应，流式和非流式均支持格式转换和故障转移 |
