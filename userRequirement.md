# API网关代理系统 - 需求文档

## 1. 项目目标

### 1.1 项目结束标志
- 实现多用户共享的API网关代理服务
- 支持动态令牌刷新和故障转移
- 提供可视化管理界面
- 实现调用统计和监控

### 1.2 用户操作方式
- **管理员**：通过Web界面配置RAPI、创建UAPI、编排路由规则、查看统计
- **终端用户**：通过标准API接口调用，只需知道UAPI别名

### 1.3 项目成果（新增能力）
| 能力 | 描述 |
|------|------|
| 统一入口 | 用户只需记住UAPI别名，无需关心后端细节 |
| 故障转移 | 自动从主服务切换到备用服务 |
| 动态令牌 | 自动刷新过期令牌，服务不中断 |
| 统计分析 | 可视化展示API使用情况 |

---

## 2. 项目概述

### 2.1 背景（为何要做）
- 企业内部存在多个LLM服务提供商（如OpenAI、火山引擎、阿里云等）
- 不同团队使用不同的API和令牌，管理复杂
- 缺少统一的入口和故障转移机制
- 令牌过期需要手动更新，影响业务连续性

### 2.2 现状（目前情况）
| 现状 | 描述 |
|------|------|
| 分散管理 | 各团队独立配置API，缺乏统一管理 |
| 单点故障 | 服务中断时需要手动切换 |
| 静态令牌 | 令牌过期需人工更新 |
| 缺乏监控 | 无法直观了解API使用情况 |

### 2.3 缺失能力
- 统一的API入口
- 自动故障转移
- 动态令牌管理
- 可视化统计监控

---

## 3. 核心需求

### 3.1 RAPI管理
| 需求 | 描述 |
|------|------|
| 添加远程API配置 | 支持配置远程服务的地址、模型名、令牌 |
| 支持动态令牌 | 支持通过外部命令获取和刷新令牌 |
| 测试验证 | 支持测试RAPI配置是否正确 |

### 3.2 UAPI管理
| 需求 | 描述 |
|------|------|
| 创建虚拟API | 为用户提供统一的接口别名 |
| 配置路由链 | 支持多个RAPI组成故障转移链 |

### 3.3 请求路由
| 需求 | 描述 |
|------|------|
| Model字段路由 | 根据请求中的model字段匹配UAPI |
| 自动故障转移 | 主服务失败时自动尝试备用服务 |
| 模型名转换 | 自动将UAPI别名替换为真实模型名 |

### 3.4 令牌管理
| 需求 | 描述 |
|------|------|
| 双重缓存 | 内存+数据库缓存令牌 |
| 定时刷新 | 后台定时更新令牌 |
| 被动刷新 | 收到401错误时立即刷新并重试 |

### 3.5 统计监控
| 需求 | 描述 |
|------|------|
| 调用统计 | 记录调用次数、成功/失败数 |
| 性能统计 | 记录响应时间、Token使用量 |
| 按UAPI汇总 | 聚合展示UAPI下所有RAPI的统计 |

---

## 4. 用户案例

### 案例1：管理员配置远程服务
**目标**：添加火山引擎LLM服务到网关
**成果**：RAPI配置完成，可被UAPI引用

### 案例2：管理员创建统一入口
**目标**：为开发团队创建统一接口
**成果**：UAPI创建完成，配置了主备服务链

### 案例3：终端用户调用
**目标**：Aider通过网关调用LLM
**成果**：请求成功响应，用户无需关心后端细节

### 案例4：故障转移生效
**目标**：主服务宕机时自动切换
**成果**：请求自动路由到备用服务，业务不中断

### 案例5：令牌自动刷新
**目标**：令牌过期时自动更新
**成果**：令牌刷新成功，服务不中断

---

## 5. 非功能需求

| 需求 | 描述 |
|------|------|
| 并发支持 | 支持100+并发请求 |
| 低延迟 | 网关处理延迟 < 500ms |
| 跨平台 | 支持Windows/Linux/macOS |
| 快速故障转移 | 切换时间 < 1秒 |
